<resources>

    <!-- Note: colors and themes must be defined in their resource files
     (colors.xml and dimens.xml) for it to be shown correctly on runtime. -->

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Overriding default theme colors: -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Overriding additional theme properties: -->
        <item name="android:colorBackground">@color/themeColorBackground</item>
        <item name="android:textSize">@dimen/themeTextSize</item>
        <item name="android:textColorHint">@color/themeTextColorHint</item>

        <!-- Setting the button style for all buttons (like Element-Name Selector): -->
        <item name="android:buttonStyle">@style/myButtonsStyle</item>

    </style>

    <!-- Button style: -->
    <style name="myButtonsStyle">
        <item name="android:background">@color/buttonBackground</item>
        <item name="android:textColor">@color/buttonTextColor</item>
        <item name="android:textSize">@dimen/buttonTextSize</item>
        <item name="android:gravity">center_horizontal</item>
    </style>

</resources>
